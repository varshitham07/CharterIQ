# CharterIQ frontend

React + Vite operator console.

Run:

```bash
npm install
npm run dev
```

Local API calls use `/api` and Vite proxies them to `http://127.0.0.1:8000`.

Production deployment can set:

```text
VITE_API_URL=https://your-api.example.com
```
